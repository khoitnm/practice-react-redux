export default class HostsResolver {
    // TODO just temporary, we'll need to get it from environment
    static practice_react_reduxServerHost = 'http://localhost:4000';
    static practice_react_reduxServerApiPrefix = HostsResolver.practice_react_reduxServerHost + '/api/v1';
}
